{% extends 'base_' ~ ga_theme ~ '.html.twig' %}

{% block title %}PolitiquePub{% endblock %}

{% block body %}
    <h1>PolitiquePub</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ politique_pub.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ politique_pub.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_politique_pub_index') }}">back to list</a>

    <a href="{{ path('admin_politique_pub_edit', {'id': politique_pub.id}) }}">edit</a>

    {{ include('admin_politique_pub/_delete_form.html.twig') }}
{% endblock %}
