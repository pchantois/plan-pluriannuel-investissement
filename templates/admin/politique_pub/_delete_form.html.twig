<form method="post" action="{{ path('admin_politique_pub_delete', {'id': politique_pub.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ politique_pub.id) }}">
    <button class="btn">Delete</button>
</form>
