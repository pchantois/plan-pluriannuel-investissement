{% extends 'base_' ~ ga_theme ~ '.html.twig' %}

{% block title %}NatureOpe{% endblock %}

{% block body %}
    <h1>NatureOpe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nature_ope.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ nature_ope.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_nature_ope_index') }}">back to list</a>

    <a href="{{ path('admin_nature_ope_edit', {'id': nature_ope.id}) }}">edit</a>

    {{ include('admin_nature_ope/_delete_form.html.twig') }}
{% endblock %}
