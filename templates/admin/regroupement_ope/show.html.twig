{% extends 'base_' ~ ga_theme ~ '.html.twig' %}

{% block title %}RegroupementOpe{% endblock %}

{% block body %}
    <h1>RegroupementOpe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ regroupement_ope.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ regroupement_ope.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_regroupement_ope_index') }}">back to list</a>

    <a href="{{ path('admin_regroupement_ope_edit', {'id': regroupement_ope.id}) }}">edit</a>

    {{ include('admin_regroupement_ope/_delete_form.html.twig') }}
{% endblock %}
